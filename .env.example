# Sanity credentials (read-only, public CDN). Leave unset to build with an
# empty post list (pages render their empty states). Copy to `.env` and fill
# in to go live.
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
PUBLIC_SANITY_API_VERSION=2024-10-01
